#include <stdio.h> 
#include <stdlib.h>
#include <fstream>
#include <iostream>

using namespace std;

struct car {
	enum key { TRUCK, BUS };
	key k; // ключ
		   // используемые альтернативы
};

//car* In(ifstream &ifst);
//void Out(car *a, ofstream &ofst);
